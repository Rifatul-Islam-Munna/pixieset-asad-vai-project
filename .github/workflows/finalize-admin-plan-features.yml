name: Finalize Admin Plan Features

on:
  pull_request:
    branches: [main]
    types: [opened, reopened]

permissions:
  contents: write

jobs:
  implement:
    if: github.head_ref == 'development'
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
        with:
          ref: development
          fetch-depth: 0

      - name: Implement admin template pages and plan feature access
        run: |
          python - <<'PY'
          from pathlib import Path

          def read(path):
              return Path(path).read_text()

          def write(path, content):
              target = Path(path)
              target.parent.mkdir(parents=True, exist_ok=True)
              target.write_text(content)

          def replace_once(text, old, new, label):
              if old not in text:
                  raise SystemExit(f"Missing expected block: {label}")
              return text.replace(old, new, 1)

          # ------------------------------------------------------------------
          # Backend: admin-managed default Store product templates.
          # ------------------------------------------------------------------
          write("backend/src/store/entities/store-default-product.entity.ts", r'''import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';
import { HydratedDocument } from 'mongoose';

export type StoreDefaultProductDocument = HydratedDocument<StoreDefaultProduct>;

@Schema({ timestamps: true, autoIndex: true })
export class StoreDefaultProduct {
  @Prop({ required: true, unique: true, index: true, trim: true })
  slug: string;

  @Prop({ required: true, trim: true })
  name: string;

  @Prop({ required: true, index: true })
  category: string;

  @Prop({ default: true, index: true })
  active: boolean;

  @Prop({ default: 0 })
  sortOrder: number;

  @Prop({ type: Object, required: true, default: {} })
  data: Record<string, unknown>;

  createdAt?: Date;
  updatedAt?: Date;
}

export const StoreDefaultProductSchema = SchemaFactory.createForClass(StoreDefaultProduct);
''')

          write("backend/src/store/store-default-product.service.ts", r'''import { Injectable, NotFoundException } from '@nestjs/common';
import { InjectModel } from '@nestjs/mongoose';
import { Model } from 'mongoose';
import { DEFAULT_STORE_PRODUCTS } from './store-defaults';
import {
  StoreDefaultProduct,
  StoreDefaultProductDocument,
} from './entities/store-default-product.entity';

@Injectable()
export class StoreDefaultProductService {
  constructor(
    @InjectModel(StoreDefaultProduct.name)
    private readonly model: Model<StoreDefaultProductDocument>,
  ) {}

  async ensureSeeded() {
    if (!DEFAULT_STORE_PRODUCTS.length) return;
    await this.model.bulkWrite(
      DEFAULT_STORE_PRODUCTS.map((item) => ({
        updateOne: {
          filter: { slug: item.slug },
          update: {
            $setOnInsert: {
              slug: item.slug,
              name: item.name,
              category: item.category,
              active: item.active !== false,
              sortOrder: item.sortOrder ?? 0,
              data: item,
            },
          },
          upsert: true,
        },
      })),
    );
  }

  async list() {
    await this.ensureSeeded();
    const rows = await this.model.find().sort({ category: 1, sortOrder: 1, createdAt: 1 }).lean();
    return rows.map((row) => this.publicRow(row));
  }

  async listActiveData() {
    const rows = await this.list();
    return rows
      .filter((row) => row.active !== false)
      .map(({ _id, createdAt, updatedAt, ...row }) => row);
  }

  async update(id: string, dto: Record<string, unknown>) {
    const product = await this.model.findById(id);
    if (!product) throw new NotFoundException('Default product template not found');
    const incoming = { ...dto } as Record<string, any>;
    delete incoming._id;
    delete incoming.createdAt;
    delete incoming.updatedAt;
    delete incoming.slug;
    delete incoming.type;

    const next = {
      ...(product.data ?? {}),
      ...incoming,
      slug: product.slug,
      type: (product.data as any)?.type ?? 'self-fulfilled',
    };
    product.name = String(incoming.name ?? product.name).trim() || product.name;
    product.category = String(incoming.category ?? product.category).trim() || product.category;
    product.active = incoming.active === undefined ? product.active : Boolean(incoming.active);
    product.sortOrder = incoming.sortOrder === undefined ? product.sortOrder : Number(incoming.sortOrder) || 0;
    product.data = {
      ...next,
      name: product.name,
      category: product.category,
      active: product.active,
      sortOrder: product.sortOrder,
    };
    product.markModified('data');
    await product.save();
    return this.publicRow(product.toObject());
  }

  private publicRow(row: any) {
    const data = { ...(row.data ?? {}) };
    return {
      _id: row._id.toString(),
      ...data,
      slug: row.slug,
      name: row.name,
      category: row.category,
      active: row.active,
      sortOrder: row.sortOrder,
      createdAt: row.createdAt,
      updatedAt: row.updatedAt,
    };
  }
}
''')

          path = "backend/src/store/store.module.ts"
          text = read(path)
          text = replace_once(text,
            "import { StoreActivity, StoreActivitySchema } from './entities/store-activity.entity';\n",
            "import { StoreActivity, StoreActivitySchema } from './entities/store-activity.entity';\nimport { StoreDefaultProduct, StoreDefaultProductSchema } from './entities/store-default-product.entity';\n",
            "store module default entity import")
          text = replace_once(text,
            "import { StorePaymentVerifyService } from './store-payment-verify.service';\n",
            "import { StorePaymentVerifyService } from './store-payment-verify.service';\nimport { StoreDefaultProductService } from './store-default-product.service';\n",
            "store module default service import")
          text = replace_once(text,
            "      { name: StoreActivity.name, schema: StoreActivitySchema },\n",
            "      { name: StoreActivity.name, schema: StoreActivitySchema },\n      { name: StoreDefaultProduct.name, schema: StoreDefaultProductSchema },\n",
            "store module schema")
          text = replace_once(text,
            "    StorePaymentVerifyService,\n  ],\n})",
            "    StorePaymentVerifyService,\n    StoreDefaultProductService,\n  ],\n  exports: [StoreDefaultProductService],\n})",
            "store module provider/export")
          write(path, text)

          path = "backend/src/store/store-catalog.service.ts"
          text = read(path)
          text = text.replace("import { DEFAULT_STORE_PRODUCTS } from './store-defaults';\n", "")
          text = replace_once(text,
            "import { StoreTax, StoreTaxDocument } from './entities/store-tax.entity';\n",
            "import { StoreTax, StoreTaxDocument } from './entities/store-tax.entity';\nimport { StoreDefaultProductService } from './store-default-product.service';\n",
            "catalog default service import")
          text = replace_once(text,
            "    @InjectModel(StoreActivity.name)\n    private readonly activityModel: Model<StoreActivityDocument>,\n  ) {}",
            "    @InjectModel(StoreActivity.name)\n    private readonly activityModel: Model<StoreActivityDocument>,\n    private readonly defaultProducts: StoreDefaultProductService,\n  ) {}",
            "catalog constructor")
          text = replace_once(text,
            "      await this.productModel.insertMany(\n        DEFAULT_STORE_PRODUCTS.map((item) => ({\n          userId,\n          priceSheetId: sheet._id.toString(),\n          ...item,\n        })),\n      );",
            "      const defaults = await this.defaultProducts.listActiveData();\n      await this.productModel.insertMany(\n        defaults.map((item: any) => ({\n          userId,\n          priceSheetId: sheet._id.toString(),\n          defaultTemplateSlug: item.slug,\n          ...item,\n        })),\n      );",
            "catalog initial defaults")
          text = replace_once(text,
            "    const missing = DEFAULT_STORE_PRODUCTS.filter((item) => !slugs.has(item.slug));\n    if (missing.length) {\n      await this.productModel.insertMany(\n        missing.map((item) => ({ userId, priceSheetId, ...item })),\n      );\n    }",
            "    const defaults = await this.defaultProducts.listActiveData();\n    const missing = defaults.filter((item: any) => !slugs.has(item.slug));\n    if (missing.length) {\n      await this.productModel.insertMany(\n        missing.map((item: any) => ({ userId, priceSheetId, defaultTemplateSlug: item.slug, ...item })),\n      );\n    }",
            "catalog ensure defaults")
          write(path, text)

          path = "backend/src/store/store.service.ts"
          text = read(path)
          text = text.replace("import { DEFAULT_STORE_PRODUCTS } from './store-defaults';\n", "")
          text = replace_once(text,
            "import { Collection, CollectionDocument } from 'src/collections/entities/collection.entity';\n",
            "import { Collection, CollectionDocument } from 'src/collections/entities/collection.entity';\nimport { StoreDefaultProductService } from './store-default-product.service';\n",
            "store service default import")
          text = replace_once(text,
            "    @InjectModel(User.name)\n    private readonly userModel: Model<UserDocument>,\n  ) {}",
            "    @InjectModel(User.name)\n    private readonly userModel: Model<UserDocument>,\n    private readonly defaultProducts: StoreDefaultProductService,\n  ) {}",
            "store service constructor")
          text = replace_once(text,
            "    if (fulfillment === 'auto') {\n      await this.productModel.insertMany(\n        DEFAULT_STORE_PRODUCTS.map((item) => ({\n          userId,\n          priceSheetId: sheet._id.toString(),\n          ...item,\n        })),\n      );\n    }",
            "    if (fulfillment === 'auto') {\n      const defaults = await this.defaultProducts.listActiveData();\n      await this.productModel.insertMany(\n        defaults.map((item: any) => ({\n          userId,\n          priceSheetId: sheet._id.toString(),\n          defaultTemplateSlug: item.slug,\n          ...item,\n        })),\n      );\n    }",
            "store service create defaults")
          write(path, text)

          path = "backend/src/store/entities/store-product.entity.ts"
          text = read(path)
          text = replace_once(text,
            "  @Prop({ default: true, index: true })\n  active: boolean;\n",
            "  @Prop({ default: true, index: true })\n  active: boolean;\n\n  @Prop({ index: true })\n  defaultTemplateSlug?: string;\n",
            "store product default template marker")
          write(path, text)

          path = "backend/src/admin/admin.module.ts"
          text = read(path)
          text = replace_once(text,
            "import { FaceSearchModule } from 'src/face-search/face-search.module';\n",
            "import { FaceSearchModule } from 'src/face-search/face-search.module';\nimport { StoreModule } from 'src/store/store.module';\n",
            "admin store module import")
          text = replace_once(text, "    FaceSearchModule,\n", "    FaceSearchModule,\n    StoreModule,\n", "admin module store import")
          write(path, text)

          path = "backend/src/admin/admin.service.ts"
          text = read(path)
          text = replace_once(text,
            "import { FaceSearchService } from 'src/face-search/face-search.service';\n",
            "import { FaceSearchService } from 'src/face-search/face-search.service';\nimport { StoreDefaultProductService } from 'src/store/store-default-product.service';\n",
            "admin default products import")
          text = replace_once(text,
            "    private readonly faceSearchService: FaceSearchService,\n  ) {}",
            "    private readonly faceSearchService: FaceSearchService,\n    private readonly defaultProducts: StoreDefaultProductService,\n  ) {}",
            "admin default products constructor")
          text = replace_once(text,
            "  async findPlans() {\n    return this.planModel.find().sort({ createdAt: -1 }).lean();\n  }\n",
            "  async findPlans() {\n    return this.planModel.find().sort({ createdAt: -1 }).lean();\n  }\n\n  async findDefaultStoreProducts() {\n    return this.defaultProducts.list();\n  }\n\n  async updateDefaultStoreProduct(id: string, dto: Record<string, unknown>) {\n    return this.defaultProducts.update(id, dto);\n  }\n",
            "admin default product methods")
          old_checkout = """    if (!plan) throw new NotFoundException('Plan not found');
    if (!settings.enabled || !settings.secretKey) throw new BadRequestException('Stripe is not configured');
    if (Number(plan.priceMonthly ?? 0) <= 0) throw new BadRequestException('Plan price is required for Stripe checkout');

    const stripe = new Stripe(settings.secretKey);"""
          new_checkout = """    if (!plan) throw new NotFoundException('Plan not found');
    if (Number(plan.priceMonthly ?? 0) <= 0) {
      const activatedPlan = await this.assignPlanToUser(userId, plan._id.toString());
      return { activated: true, checkoutUrl: null, sessionId: null, plan: activatedPlan };
    }
    if (!settings.enabled || !settings.secretKey) throw new BadRequestException('Stripe is not configured');

    const stripe = new Stripe(settings.secretKey);"""
          text = replace_once(text, old_checkout, new_checkout, "free plan activation")
          text = replace_once(text,
            "    const user = await this.userModel.findById(userId).select('monthlyEmailLimit monthlyEmailsUsed monthlyUsageKey').lean();\n",
            "    const user = await this.userModel.findById(userId).select('monthlyEmailLimit monthlyEmailsUsed monthlyUsageKey planFeatures').lean();\n    if (!user?.planFeatures?.marketingEmails) {\n      throw new BadRequestException('Marketing email is not included in your current plan.');\n    }\n",
            "marketing email feature backend")
          write(path, text)

          path = "backend/src/admin/admin.controller.ts"
          text = read(path)
          text = replace_once(text,
            "  @Get('plans')\n  async plans() {",
            "  @Get('default-store-products')\n  async defaultStoreProducts() {\n    const data = await this.adminService.findDefaultStoreProducts();\n    return { data };\n  }\n\n  @Patch('default-store-products/:id')\n  async updateDefaultStoreProduct(@Param('id') id: string, @Body() dto: Record<string, unknown>) {\n    const data = await this.adminService.updateDefaultStoreProduct(id, dto);\n    return { message: 'Default product updated', data };\n  }\n\n  @Get('plans')\n  async plans() {",
            "admin default product endpoints")
          write(path, text)

          path = "backend/src/admin/entities/plan.entity.ts"
          text = read(path)
          text = replace_once(text,
            "    advancedDesign?: boolean;\n",
            "    advancedDesign?: boolean;\n    customCover?: boolean;\n",
            "custom cover plan entity")
          write(path, text)

          # Backend collection enforcement for all plan-controlled features.
          path = "backend/src/collections/collections.service.ts"
          text = read(path)
          text = replace_once(text,
            "  async create(userId: string, dto: CreateCollectionDto) {\n    const collection = await this.collectionModel.create({\n      userId,\n      name: dto.name,\n      slug: await this.uniqueSlug(userId, dto.name),\n      eventDate: dto.eventDate ? new Date(dto.eventDate) : undefined,\n      presetId: dto.presetId,\n      design: dto.design ?? {},\n      settings: dto.settings ?? {},",
            "  async create(userId: string, dto: CreateCollectionDto) {\n    const safeDto = await this.sanitizeCollectionCapabilities(userId, dto);\n    const collection = await this.collectionModel.create({\n      userId,\n      name: safeDto.name,\n      slug: await this.uniqueSlug(userId, safeDto.name),\n      eventDate: safeDto.eventDate ? new Date(safeDto.eventDate) : undefined,\n      presetId: safeDto.presetId,\n      design: safeDto.design ?? {},\n      settings: safeDto.settings ?? {},",
            "sanitize create collection")
          start = text.index("  private async sanitizeCollectionCapabilities(")
          end = text.index("\n  }\n}\n\nfunction minDate", start) + len("\n  }")
          replacement = r'''  private async sanitizeCollectionCapabilities<T extends CreateCollectionDto | UpdateCollectionDto>(userId: string, dto: T): Promise<T> {
    const user = await this.userModel.findById(userId).select('planFeatures').lean();
    const features = user?.planFeatures ?? {};
    const next: any = { ...dto };
    const settings = { ...((next.settings ?? {}) as any) };
    const download = { ...(settings.download ?? {}) };
    const store = { ...(settings.store ?? {}) };
    const design = { ...((next.design ?? {}) as any) };

    if (next.coverImage && !features.coverImage) delete next.coverImage;

    if (!features.customCover) {
      delete design.customCoverTemplate;
      if (String(design.cover ?? '').startsWith('custom:')) design.cover = 'Center';
    }

    if (!features.advancedDesign) {
      delete design.typography;
      delete design.customFontName;
      delete design.customFontDataUrl;
      delete design.color;
      delete design.navigationStyle;
    }

    if (!features.layouts) {
      design.gridStyle = 'Vertical';
      design.thumbnailSize = 'Regular';
      design.gridSpacing = 'Regular';
    }

    if ((download.limitDownloads || download.restrictDownloads) && !features.downloadLimit) {
      download.limitDownloads = false;
      download.restrictDownloads = false;
      download.limitPinUsage = '';
    }
    if (download.downloadPin && !features.pinSet) {
      download.downloadPin = false;
      download.downloadPinCode = '';
    }
    if (!features.store) {
      store.enabled = false;
      store.storeStatus = false;
      store.showPrintStoreNav = false;
      store.showBuyPhotoButton = false;
    }

    if (next.design) next.design = design;
    if (next.settings) next.settings = { ...settings, download, store };
    return next as T;
  }'''
          text = text[:start] + replacement + text[end:]
          write(path, text)

          # ------------------------------------------------------------------
          # Frontend server actions and API capability proxy.
          # ------------------------------------------------------------------
          path = "frontend/actions/admin.ts"
          text = read(path)
          text = replace_once(text,
            "export type AdminStripeSetting = {",
            r'''export type AdminDefaultStoreVariant = {
  id: string;
  label: string;
  options?: Record<string, string>;
  price: number;
  extraShipping?: number;
  hidden?: boolean;
  sortOrder?: number;
  isDefault?: boolean;
};

export type AdminDefaultStoreProduct = {
  _id: string;
  slug: string;
  type: "digital-download" | "self-fulfilled";
  name: string;
  category: string;
  active: boolean;
  sortOrder: number;
  description?: string;
  productInfo?: string;
  productionNote?: string;
  price: number;
  extraShipping?: number;
  images?: string[];
  previewImages?: string[];
  variants?: AdminDefaultStoreVariant[];
  options?: { name: string; values: string[] }[];
  updatedAt?: string;
};

export type AdminStripeSetting = {''',
            "admin default product types")
          text += r'''

export async function getAdminHomeCms() {
  return adminRequest<HomeCmsData>("/home-cms");
}

export async function getAdminDefaultStoreProducts() {
  return adminRequest<AdminDefaultStoreProduct[]>("/admin/default-store-products");
}

export async function updateAdminDefaultStoreProduct(id: string, payload: Partial<AdminDefaultStoreProduct>) {
  return adminRequest<AdminDefaultStoreProduct>(`/admin/default-store-products/${id}`, {
    method: "PATCH",
    body: JSON.stringify(payload),
  });
}
'''
          write(path, text)

          write("frontend/app/api/billing/capabilities/route.ts", r'''import { cookies } from "next/headers";
import { NextResponse } from "next/server";

const baseUrl = process.env.BASE_URL ?? process.env.NEXT_PUBLIC_BASE_URL ?? "http://localhost:4000";

export async function GET() {
  const token = (await cookies()).get("access_token")?.value ?? "";
  if (!token) return NextResponse.json({ message: "Login required" }, { status: 401 });
  const response = await fetch(`${baseUrl}/billing/capabilities`, {
    headers: { access_token: token },
    cache: "no-store",
  });
  const payload = await response.json().catch(() => null);
  return NextResponse.json(payload, { status: response.status });
}
''')

          write("frontend/api-hooks/use-plan-capabilities.ts", r'''"use client";

import { useQuery } from "@tanstack/react-query";

export type PlanFeatureKey =
  | "pinSet"
  | "downloadLimit"
  | "coverImage"
  | "layouts"
  | "advancedDesign"
  | "customCover"
  | "store"
  | "marketingEmails";

export type PlanCapabilities = {
  planName: string;
  storageLimitGb: number;
  monthlyEmailLimit: number;
  features: Partial<Record<PlanFeatureKey, boolean>>;
};

export function usePlanCapabilities() {
  return useQuery<{ data: PlanCapabilities }>({
    queryKey: ["billing-capabilities"],
    queryFn: async () => {
      const response = await fetch("/api/billing/capabilities", { cache: "no-store" });
      const payload = await response.json().catch(() => null);
      if (!response.ok) throw new Error(payload?.message ?? "Could not load plan features");
      return payload;
    },
    staleTime: 60_000,
    retry: false,
  });
}

export function usePlanFeatureAccess(feature: PlanFeatureKey) {
  const query = usePlanCapabilities();
  const loading = query.isLoading;
  const enabled = loading || Boolean(query.data?.data?.features?.[feature]);
  return { enabled, locked: !loading && !enabled, loading, capabilities: query.data?.data };
}
''')

          write("frontend/components/dashboard/plan-feature-lock.tsx", r'''"use client";

import { Lock } from "lucide-react";
import Link from "next/link";
import type { ReactNode } from "react";
import { usePlanFeatureAccess, type PlanFeatureKey } from "@/api-hooks/use-plan-capabilities";
import { cn } from "@/lib/utils";

export function PlanFeatureLock({
  feature,
  label,
  children,
  compact = false,
  className,
}: {
  feature: PlanFeatureKey;
  label: string;
  children: ReactNode;
  compact?: boolean;
  className?: string;
}) {
  const { locked, capabilities } = usePlanFeatureAccess(feature);
  return (
    <div className={cn("relative", className)}>
      <div className={cn(locked && "pointer-events-none select-none opacity-45")}>{children}</div>
      {locked && (
        <div className={cn("absolute inset-0 z-20 flex items-center justify-center bg-white/38 backdrop-blur-[1px]", compact && "items-start justify-end bg-transparent p-2 backdrop-blur-none")}>
          <div className={cn("max-w-sm border bg-white p-5 text-center shadow-[0_18px_50px_rgba(0,0,0,0.16)]", compact && "max-w-none rounded-full border-0 bg-[#111] px-3 py-1.5 text-left text-white shadow-none")}>
            <Lock className={cn("mx-auto size-5", compact && "mr-1.5 inline size-3.5")} />
            <p className={cn("mt-3 text-sm font-bold", compact && "mt-0 inline text-xs")}>{label} is not included in {capabilities?.planName || "your plan"}</p>
            {!compact && <Link href="/dashboard/client-gallery/storage" className="mt-3 inline-flex text-sm font-bold text-[#00a997]">View plans</Link>}
          </div>
        </div>
      )}
    </div>
  );
}

export function PlanFeatureNotice({ feature, label }: { feature: PlanFeatureKey; label: string }) {
  const { locked, capabilities } = usePlanFeatureAccess(feature);
  if (!locked) return null;
  return (
    <div className="mb-5 flex items-center gap-3 border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">
      <Lock className="size-4 shrink-0" />
      <span><b>{label}</b> is visible but disabled on {capabilities?.planName || "your current plan"}. <Link href="/dashboard/client-gallery/storage" className="font-bold underline">Upgrade</Link></span>
    </div>
  );
}
''')

          # Billing action supports instant free activation.
          path = "frontend/actions/billing.ts"
          text = read(path)
          text = replace_once(text,
            "  monthlyEmailsUsed?: number;\n};",
            "  monthlyEmailsUsed?: number;\n  planFeatures?: Record<string, boolean>;\n};",
            "billing user features")
          text = replace_once(text,
            "  const data = await authedRequest<{ checkoutUrl?: string }>(`/billing/plans/${planId}/checkout`, {",
            "  const data = await authedRequest<{ checkoutUrl?: string | null; activated?: boolean; plan?: AdminPlan }>(`/billing/plans/${planId}/checkout`, {",
            "free plan checkout return")
          write(path, text)

          # ------------------------------------------------------------------
          # Shared admin shell and dedicated admin pages.
          # ------------------------------------------------------------------
          write("frontend/components/dashboard/admin-resource-shell.tsx", r'''"use client";

import Link from "next/link";
import { BarChart3, FileImage, Package, ShoppingBag } from "lucide-react";
import type { ReactNode } from "react";
import { cn } from "@/lib/utils";

const links = [
  { href: "/admin", label: "Admin dashboard", icon: BarChart3 },
  { href: "/admin/cover-templates", label: "Cover templates", icon: FileImage },
  { href: "/admin/default-products", label: "Default products", icon: ShoppingBag },
];

export function AdminResourceShell({ active, title, subtitle, action, children }: {
  active: "covers" | "products";
  title: string;
  subtitle: string;
  action?: ReactNode;
  children: ReactNode;
}) {
  const activeHref = active === "covers" ? "/admin/cover-templates" : "/admin/default-products";
  return (
    <main className="min-h-screen bg-[#f3f3f1] text-[#171717]">
      <div className="grid min-h-screen lg:grid-cols-[250px_1fr]">
        <aside className="border-r bg-white p-5 lg:p-6">
          <Link href="/admin" className="flex items-center gap-3 text-lg font-bold"><span className="flex size-9 items-center justify-center bg-[#111] text-white"><Package className="size-4" /></span>Admin</Link>
          <nav className="mt-10 grid gap-2">
            {links.map((item) => <Link key={item.href} href={item.href} className={cn("flex h-11 items-center gap-3 px-3 text-sm font-bold", item.href === activeHref ? "bg-[#111] text-white" : "text-[#555] hover:bg-[#f3f3f3]")}><item.icon className="size-4" />{item.label}</Link>)}
          </nav>
        </aside>
        <section className="min-w-0 p-4 sm:p-7 lg:p-10">
          <div className="flex flex-wrap items-start justify-between gap-5 border-b pb-6">
            <div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#777]">Admin management</p><h1 className="mt-2 text-3xl font-semibold">{title}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-[#666]">{subtitle}</p></div>
            {action}
          </div>
          <div className="mt-7">{children}</div>
        </section>
      </div>
    </main>
  );
}
''')

          write("frontend/app/admin/cover-templates/page.tsx", r'''import { redirect } from "next/navigation";
import { getAdminHomeCms } from "@/actions/admin";
import { getUser } from "@/actions/auth";
import { AdminCoverTemplatesPage } from "@/components/dashboard/admin-cover-templates-page";
import { mergeHomeCms } from "@/lib/home-cms";

export default async function Page() {
  const user = await getUser();
  if (!user) redirect("/login");
  if (user.role !== "admin") redirect("/dashboard/client-gallery");
  const homeCms = await getAdminHomeCms();
  return <AdminCoverTemplatesPage initialCms={mergeHomeCms(homeCms)} />;
}
''')

          write("frontend/components/dashboard/admin-cover-templates-page.tsx", r'''"use client";

import { useMemo, useState, useTransition } from "react";
import { Edit3, Loader2, PlusCircle, Save, Trash2, Upload, X } from "lucide-react";
import { toast } from "sonner";
import { updateHomeCms, uploadHomeCmsFile } from "@/actions/admin";
import { CoverPreview } from "@/components/dashboard/cover-designs";
import { AdminResourceShell } from "./admin-resource-shell";
import type { CustomCoverElement, CustomCoverTemplate, HomeCmsData } from "@/lib/home-cms";

const blank = (): CustomCoverTemplate => ({
  id: `cover-${Date.now()}`,
  name: "New custom cover",
  backgroundImage: "https://images.unsplash.com/photo-1519741497674-611481863552?auto=format&fit=crop&w=1400&q=80",
  overlayOpacity: 24,
  gridOpacity: 0,
  lineOpacity: 70,
  elements: [{ id: `title-${Date.now()}`, type: "title", text: "COLLECTION TITLE", x: 50, y: 48, width: 70, height: 14, fontSize: 34, color: "#ffffff", opacity: 100, align: "center" }],
});

export function AdminCoverTemplatesPage({ initialCms }: { initialCms: HomeCmsData }) {
  const [cms, setCms] = useState(initialCms);
  const [draft, setDraft] = useState<CustomCoverTemplate | null>(null);
  const [pending, startTransition] = useTransition();
  const templates = cms.coverTemplates ?? [];
  const sorted = useMemo(() => [...templates].sort((a, b) => a.name.localeCompare(b.name)), [templates]);

  const persist = (next: CustomCoverTemplate[], success: string) => startTransition(async () => {
    try {
      const saved = await updateHomeCms({ ...cms, coverTemplates: next });
      setCms(saved);
      setDraft(null);
      toast.success(success);
    } catch (error) { toast.error(error instanceof Error ? error.message : "Could not save cover templates"); }
  });
  const save = () => {
    if (!draft?.name.trim()) return toast.error("Template name is required");
    const exists = templates.some((item) => item.id === draft.id);
    persist(exists ? templates.map((item) => item.id === draft.id ? draft : item) : [...templates, draft], exists ? "Cover template updated" : "Cover template created");
  };
  const remove = (template: CustomCoverTemplate) => {
    if (!confirm(`Delete ${template.name}?`)) return;
    persist(templates.filter((item) => item.id !== template.id), "Cover template deleted");
  };
  const patchElement = (id: string, value: Partial<CustomCoverElement>) => setDraft((current) => current ? ({ ...current, elements: current.elements.map((item) => item.id === id ? { ...item, ...value } : item) }) : current);
  const addElement = (type: CustomCoverElement["type"]) => setDraft((current) => current ? ({ ...current, elements: [...current.elements, { id: `${type}-${Date.now()}`, type, text: type === "button" ? "VIEW GALLERY" : type === "date" ? "EVENT DATE" : type === "line" ? "" : type.toUpperCase(), x: 50, y: 55, width: type === "line" ? 55 : 60, height: type === "line" ? 1 : 9, fontSize: type === "title" ? 32 : 14, color: "#ffffff", opacity: 100, align: "center" }] }) : current);

  return (
    <AdminResourceShell active="covers" title="Client gallery cover templates" subtitle="Create templates one by one, then edit or delete them from this table. These templates appear in the collection cover designer only when the user plan includes Custom cover." action={<button onClick={() => setDraft(blank())} className="inline-flex h-11 items-center gap-2 bg-[#111] px-5 text-sm font-bold text-white"><PlusCircle className="size-4" />Add template</button>}>
      <div className="overflow-x-auto bg-white"><table className="w-full min-w-[760px] text-sm"><thead className="border-b bg-[#fafafa] text-left text-xs uppercase tracking-[0.14em] text-[#777]"><tr><th className="px-4 py-4">Preview</th><th className="px-4 py-4">Template</th><th className="px-4 py-4">Layers</th><th className="px-4 py-4">Background</th><th className="px-4 py-4 text-right">Actions</th></tr></thead><tbody>
        {sorted.map((template) => <tr key={template.id} className="border-b"><td className="w-44 px-4 py-4"><div className="aspect-[1.45] overflow-hidden"><CoverPreview design={{ cover: `custom:${template.id}`, customCoverTemplate: template }} compact className="min-h-0" /></div></td><td className="px-4 py-4"><b>{template.name}</b><p className="mt-1 text-xs text-[#777]">{template.id}</p></td><td className="px-4 py-4">{template.elements.length}</td><td className="max-w-52 truncate px-4 py-4 text-xs text-[#666]">{template.backgroundImage}</td><td className="px-4 py-4"><div className="flex justify-end gap-2"><button onClick={() => setDraft(JSON.parse(JSON.stringify(template)))} className="p-2 hover:bg-[#f2f2f2]" aria-label="Edit template"><Edit3 className="size-4" /></button><button onClick={() => remove(template)} disabled={pending} className="p-2 text-red-600 hover:bg-red-50" aria-label="Delete template"><Trash2 className="size-4" /></button></div></td></tr>)}
        {!sorted.length && <tr><td colSpan={5} className="px-4 py-16 text-center text-[#777]">No cover templates yet. Add the first template.</td></tr>}
      </tbody></table></div>
      {draft && <div className="fixed inset-0 z-50 overflow-y-auto bg-black/55 p-4"><div className="mx-auto my-4 w-full max-w-6xl bg-white p-5 shadow-2xl sm:p-7"><div className="flex items-start justify-between border-b pb-5"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#777]">Cover template editor</p><h2 className="mt-2 text-2xl font-semibold">{draft.name}</h2></div><button onClick={() => setDraft(null)}><X className="size-5" /></button></div>
        <div className="mt-6 grid gap-7 lg:grid-cols-[minmax(0,1fr)_360px]"><div><div className="aspect-[1.55] overflow-hidden bg-[#111]"><CoverPreview design={{ cover: `custom:${draft.id}`, customCoverTemplate: draft }} className="min-h-full" /></div><div className="mt-4 flex flex-wrap gap-2">{(["title","subtitle","date","button","brandText","logo","line"] as CustomCoverElement["type"][]).map((type) => <button key={type} onClick={() => addElement(type)} className="border px-3 py-2 text-xs font-bold capitalize">+ {type}</button>)}</div>
          <div className="mt-6 grid gap-3">{draft.elements.map((element) => <div key={element.id} className="grid gap-3 border bg-[#fafafa] p-3 sm:grid-cols-6"><label className="grid gap-1 sm:col-span-2"><span className="text-[10px] font-bold uppercase text-[#777]">Text / type</span><input value={element.text} onChange={(e) => patchElement(element.id,{text:e.target.value})} className="h-10 border px-3 text-sm" placeholder={element.type} /></label>{(["x","y","width","fontSize"] as const).map((key) => <label key={key} className="grid gap-1"><span className="text-[10px] font-bold uppercase text-[#777]">{key}</span><input type="number" value={element[key]} onChange={(e) => patchElement(element.id,{[key]:Number(e.target.value)} as any)} className="h-10 border px-2 text-sm" /></label>)}<div className="flex items-end"><button onClick={() => setDraft({...draft,elements:draft.elements.filter((item)=>item.id!==element.id)})} className="h-10 w-full text-red-600"><Trash2 className="mx-auto size-4" /></button></div></div>)}</div>
        </div><div className="grid content-start gap-4"><Field label="Template name" value={draft.name} onChange={(name)=>setDraft({...draft,name})} /><Field label="Background URL" value={draft.backgroundImage} onChange={(backgroundImage)=>setDraft({...draft,backgroundImage})} /><label className="flex h-11 cursor-pointer items-center justify-center gap-2 bg-[#111] text-sm font-bold text-white"><Upload className="size-4" />Upload background<input type="file" accept="image/*" className="hidden" onChange={async(e)=>{const file=e.target.files?.[0]; if(!file)return; try{const url=await uploadHomeCmsFile(file);setDraft({...draft,backgroundImage:url});}catch(error){toast.error(error instanceof Error?error.message:"Upload failed");}}}/></label><Field label="Overlay opacity" value={String(draft.overlayOpacity)} onChange={(value)=>setDraft({...draft,overlayOpacity:Number(value)||0})} type="number" /><Field label="Grid opacity" value={String(draft.gridOpacity)} onChange={(value)=>setDraft({...draft,gridOpacity:Number(value)||0})} type="number" /><button onClick={save} disabled={pending} className="mt-2 inline-flex h-12 items-center justify-center gap-2 bg-[#22bda7] text-sm font-bold text-white disabled:opacity-50">{pending?<Loader2 className="size-4 animate-spin"/>:<Save className="size-4"/>}Save template</button></div></div>
      </div></div>}
    </AdminResourceShell>
  );
}

function Field({label,value,onChange,type="text"}:{label:string;value:string;onChange:(value:string)=>void;type?:string}){return <label className="grid gap-2"><span className="text-xs font-bold uppercase tracking-[0.12em] text-[#777]">{label}</span><input type={type} value={value} onChange={(e)=>onChange(e.target.value)} className="h-11 border px-3 text-sm outline-none focus:border-[#22bda7]"/></label>}
''')

          write("frontend/app/admin/default-products/page.tsx", r'''import { redirect } from "next/navigation";
import { getAdminDefaultStoreProducts } from "@/actions/admin";
import { getUser } from "@/actions/auth";
import { AdminDefaultProductsPage } from "@/components/dashboard/admin-default-products-page";

export default async function Page() {
  const user = await getUser();
  if (!user) redirect("/login");
  if (user.role !== "admin") redirect("/dashboard/client-gallery");
  const products = await getAdminDefaultStoreProducts();
  return <AdminDefaultProductsPage initialProducts={products} />;
}
''')

          write("frontend/components/dashboard/admin-default-products-page.tsx", r'''"use client";

import { useMemo, useState, useTransition } from "react";
import { Edit3, Loader2, Save, Upload, X } from "lucide-react";
import { toast } from "sonner";
import { type AdminDefaultStoreProduct, updateAdminDefaultStoreProduct, uploadHomeCmsFile } from "@/actions/admin";
import { AdminResourceShell } from "./admin-resource-shell";

export function AdminDefaultProductsPage({ initialProducts }: { initialProducts: AdminDefaultStoreProduct[] }) {
  const [products, setProducts] = useState(initialProducts);
  const [filter, setFilter] = useState<"All" | "Prints" | "Wall Art">("All");
  const [draft, setDraft] = useState<AdminDefaultStoreProduct | null>(null);
  const [pending, startTransition] = useTransition();
  const visible = useMemo(() => products.filter((item) => ["Prints","Wall Art"].includes(item.category) && (filter === "All" || item.category === filter)).sort((a,b)=>(a.sortOrder??0)-(b.sortOrder??0)), [products, filter]);
  const save = () => {
    if (!draft) return;
    startTransition(async()=>{try{const saved=await updateAdminDefaultStoreProduct(draft._id,draft);setProducts((current)=>current.map((item)=>item._id===saved._id?saved:item));setDraft(null);toast.success("Default product updated");}catch(error){toast.error(error instanceof Error?error.message:"Update failed");}});
  };
  const upload = async(file?:File)=>{if(!file||!draft)return;try{const url=await uploadHomeCmsFile(file);setDraft({...draft,images:[url],previewImages:[url]});}catch(error){toast.error(error instanceof Error?error.message:"Upload failed");}};
  return <AdminResourceShell active="products" title="Default Prints & Wall Art" subtitle="Edit the admin-owned product templates, prices, variants and product images used when a default Store catalog is generated. Users can use these defaults, but they cannot create or delete the master templates.">
    <div className="mb-5 flex flex-wrap gap-2">{(["All","Prints","Wall Art"] as const).map((item)=><button key={item} onClick={()=>setFilter(item)} className={`h-10 border px-5 text-sm font-bold ${filter===item?"bg-[#111] text-white":"bg-white"}`}>{item}</button>)}</div>
    <div className="overflow-x-auto bg-white"><table className="w-full min-w-[850px] text-sm"><thead className="border-b bg-[#fafafa] text-left text-xs uppercase tracking-[0.14em] text-[#777]"><tr><th className="px-4 py-4">Image</th><th className="px-4 py-4">Product</th><th className="px-4 py-4">Category</th><th className="px-4 py-4">Base price</th><th className="px-4 py-4">Variants</th><th className="px-4 py-4">Status</th><th className="px-4 py-4 text-right">Action</th></tr></thead><tbody>{visible.map((product)=><tr key={product._id} className="border-b"><td className="px-4 py-4"><div className="size-20 overflow-hidden bg-[#eee]">{product.images?.[0]?<img src={product.images[0]} alt="" className="h-full w-full object-cover"/>:null}</div></td><td className="px-4 py-4"><b>{product.name}</b><p className="mt-1 text-xs text-[#777]">{product.slug}</p></td><td className="px-4 py-4">{product.category}</td><td className="px-4 py-4">${Number(product.price||0).toFixed(2)}</td><td className="px-4 py-4">{product.variants?.length??0}</td><td className="px-4 py-4">{product.active?"Active":"Inactive"}</td><td className="px-4 py-4 text-right"><button onClick={()=>setDraft(JSON.parse(JSON.stringify(product)))} className="p-2 hover:bg-[#f3f3f3]"><Edit3 className="size-4"/></button></td></tr>)}</tbody></table></div>
    {draft&&<div className="fixed inset-0 z-50 overflow-y-auto bg-black/55 p-4"><div className="mx-auto my-4 w-full max-w-4xl bg-white p-6 shadow-2xl"><div className="flex justify-between border-b pb-5"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#777]">Default product editor</p><h2 className="mt-2 text-2xl font-semibold">{draft.name}</h2></div><button onClick={()=>setDraft(null)}><X className="size-5"/></button></div><div className="mt-6 grid gap-6 md:grid-cols-2"><Field label="Product name" value={draft.name} onChange={(name)=>setDraft({...draft,name})}/><Field label="Category" value={draft.category} onChange={(category)=>setDraft({...draft,category})}/><Field label="Base price" value={String(draft.price??0)} onChange={(value)=>setDraft({...draft,price:Number(value)||0})} type="number"/><Field label="Extra shipping" value={String(draft.extraShipping??0)} onChange={(value)=>setDraft({...draft,extraShipping:Number(value)||0})} type="number"/><Area label="Description" value={draft.description??""} onChange={(description)=>setDraft({...draft,description})}/><Area label="Product information" value={draft.productInfo??""} onChange={(productInfo)=>setDraft({...draft,productInfo})}/><Area label="Production note" value={draft.productionNote??""} onChange={(productionNote)=>setDraft({...draft,productionNote})}/><div><p className="text-xs font-bold uppercase tracking-[0.12em] text-[#777]">Product image</p><div className="mt-2 flex items-center gap-4">{draft.images?.[0]&&<img src={draft.images[0]} alt="" className="size-28 object-cover"/>}<label className="inline-flex h-11 cursor-pointer items-center gap-2 bg-[#111] px-4 text-sm font-bold text-white"><Upload className="size-4"/>Upload<input type="file" accept="image/*" className="hidden" onChange={(e)=>upload(e.target.files?.[0])}/></label></div></div></div>
      <div className="mt-7"><div className="flex items-center justify-between"><h3 className="font-bold">Size / option variants</h3><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={draft.active} onChange={(e)=>setDraft({...draft,active:e.target.checked})}/>Active template</label></div><div className="mt-3 max-h-80 overflow-y-auto border">{(draft.variants??[]).map((variant,index)=><div key={variant.id} className="grid gap-3 border-b p-3 sm:grid-cols-[1.5fr_120px_120px_80px]"><input value={variant.label} onChange={(e)=>setDraft({...draft,variants:draft.variants?.map((item,i)=>i===index?{...item,label:e.target.value}:item)})} className="h-10 border px-3 text-sm"/><input type="number" value={variant.price} onChange={(e)=>setDraft({...draft,variants:draft.variants?.map((item,i)=>i===index?{...item,price:Number(e.target.value)||0}:item)})} className="h-10 border px-3 text-sm"/><input type="number" value={variant.extraShipping??0} onChange={(e)=>setDraft({...draft,variants:draft.variants?.map((item,i)=>i===index?{...item,extraShipping:Number(e.target.value)||0}:item)})} className="h-10 border px-3 text-sm"/><label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={!variant.hidden} onChange={(e)=>setDraft({...draft,variants:draft.variants?.map((item,i)=>i===index?{...item,hidden:!e.target.checked}:item)})}/>Visible</label></div>)}</div></div><div className="mt-7 flex justify-end gap-3"><button onClick={()=>setDraft(null)} className="h-11 border px-5 text-sm font-bold">Cancel</button><button onClick={save} disabled={pending} className="inline-flex h-11 items-center gap-2 bg-[#22bda7] px-6 text-sm font-bold text-white">{pending?<Loader2 className="size-4 animate-spin"/>:<Save className="size-4"/>}Save product</button></div></div></div>}
  </AdminResourceShell>;
}
function Field({label,value,onChange,type="text"}:{label:string;value:string;onChange:(value:string)=>void;type?:string}){return <label className="grid gap-2"><span className="text-xs font-bold uppercase tracking-[0.12em] text-[#777]">{label}</span><input type={type} value={value} onChange={(e)=>onChange(e.target.value)} className="h-11 border px-3 text-sm"/></label>}
function Area({label,value,onChange}:{label:string;value:string;onChange:(value:string)=>void}){return <label className="grid gap-2"><span className="text-xs font-bold uppercase tracking-[0.12em] text-[#777]">{label}</span><textarea value={value} onChange={(e)=>onChange(e.target.value)} className="min-h-28 border p-3 text-sm"/></label>}
''')

          # ------------------------------------------------------------------
          # Admin dashboard: links to separate pages and Custom cover plan flag.
          # ------------------------------------------------------------------
          path = "frontend/components/dashboard/admin-dashboard.tsx"
          text = read(path)
          text = replace_once(text, 'import { useRouter } from "next/navigation";\n', 'import { useRouter } from "next/navigation";\nimport Link from "next/link";\n', "admin Link import")
          text = text.replace("type ComponentType, type FormEvent, type PointerEvent, type ReactNode", "type ComponentType, type FormEvent, type ReactNode")
          text = text.replace("mergeHomeCms, type CustomCoverElement, type CustomCoverTemplate, type GalleryTab", "mergeHomeCms, type GalleryTab")
          text = replace_once(text, '  ["advancedDesign", "Advanced design"],\n', '  ["advancedDesign", "Advanced design"],\n  ["customCover", "Custom cover"],\n', "custom cover plan checkbox")
          cms_block = '''        <CmsSection title="Client gallery cover templates" defaultOpen>
          <AdminCoverTemplateBuilder
            templates={form.coverTemplates}
            setTemplates={setCoverTemplates}
            onUpload={onUpload}
            busy={busy}
          />
        </CmsSection>

'''
          text = replace_once(text, cms_block, '', "remove cover builder from home cms")
          start = text.index("const blankCoverTemplate =")
          end = text.index("function CmsRepeater", start)
          text = text[:start] + text[end:]
          old_nav_end = '''      {items.map((item) => (
        <button key={item.id} className={navClass(tab === item.id)} onClick={() => setTab(item.id)}>
          <item.icon className="size-4" />
          {item.label}
        </button>
      ))}
    </nav>'''
          new_nav_end = '''      {items.map((item) => (
        <button key={item.id} className={navClass(tab === item.id)} onClick={() => setTab(item.id)}>
          <item.icon className="size-4" />
          {item.label}
        </button>
      ))}
      <div className="my-2 border-t" />
      <Link href="/admin/cover-templates" className={navClass(false)}><FileImage className="size-4" />Cover Templates</Link>
      <Link href="/admin/default-products" className={navClass(false)}><ShoppingBag className="size-4" />Default Products</Link>
    </nav>'''
          text = replace_once(text, old_nav_end, new_nav_end, "admin resource links")
          # Remove no-longer-needed setter if present.
          text = text.replace('  const setCoverTemplates = (coverTemplates: CustomCoverTemplate[]) => setForm({ ...form, coverTemplates });\n', '')
          write(path, text)

          # ------------------------------------------------------------------
          # Client dashboard feature locks and zero-price UX.
          # ------------------------------------------------------------------
          path = "frontend/components/dashboard/client-dashboard.tsx"
          text = read(path)
          text = replace_once(text,
            'import { cn } from "@/lib/utils";\n',
            'import { cn } from "@/lib/utils";\nimport { usePlanFeatureAccess } from "@/api-hooks/use-plan-capabilities";\nimport { PlanFeatureLock, PlanFeatureNotice } from "@/components/dashboard/plan-feature-lock";\n',
            "client plan feature imports")

          # Wrap all Store pages and Marketing in a visible-but-disabled boundary.
          text = replace_once(text,
            '''          {campaignBuilderOpen ? (
            <CampaignBuilder onClose={closeCampaignBuilder} />''',
            '''          <PlanFeatureLock
            feature={section === "store-gallery" ? "store" : "marketingEmails"}
            label={section === "store-gallery" ? "Store" : "Marketing email"}
            className={section === "store-gallery" || page === "marketing" || campaignBuilderOpen ? "min-h-[520px]" : "contents"}
          >
          {campaignBuilderOpen ? (
            <CampaignBuilder onClose={closeCampaignBuilder} />''',
            "dashboard feature boundary open")
          text = replace_once(text,
            '''          )}
        </div>
      </section>''',
            '''          )}
          </PlanFeatureLock>
        </div>
      </section>''',
            "dashboard feature boundary close")
          # The generic boundary should only lock when required; make the component no-op for ordinary client pages by using marketing only condition.
          text = text.replace('feature={section === "store-gallery" ? "store" : "marketingEmails"}\n            label={section === "store-gallery" ? "Store" : "Marketing email"}\n            className={section === "store-gallery" || page === "marketing" || campaignBuilderOpen ? "min-h-[520px]" : "contents"}', 'feature={section === "store-gallery" ? "store" : "marketingEmails"}\n            label={section === "store-gallery" ? "Store" : "Marketing email"}\n            className={section === "store-gallery" || page === "marketing" || campaignBuilderOpen ? "min-h-[520px]" : "contents"}\n            bypass={section !== "store-gallery" && page !== "marketing" && !campaignBuilderOpen}')

          # Custom cover, advanced design and layout gating in shared collection design panel.
          text = replace_once(text,
            "}) {\n  const [adminCoverTemplates, setAdminCoverTemplates] = useState<CustomCoverTemplate[]>([]);\n  const readCustomFont",
            "}) {\n  const [adminCoverTemplates, setAdminCoverTemplates] = useState<CustomCoverTemplate[]>([]);\n  const customCoverAccess = usePlanFeatureAccess(\"customCover\");\n  const advancedDesignAccess = usePlanFeatureAccess(\"advancedDesign\");\n  const layoutsAccess = usePlanFeatureAccess(\"layouts\");\n  const readCustomFont",
            "design feature access")
          text = replace_once(text,
            '''            <p className="mt-10 text-sm font-bold">Cover</p>
            <div className="mt-5 grid grid-cols-2 gap-x-4 gap-y-8">
              {adminCoverTemplates.map((template) => (
                <button
                  key={template.id}
                  className="text-center"
                  onClick={() => onChange({ cover: `custom:${template.id}`, customCoverTemplate: template } as Partial<typeof design>)
                  type="button"
                >''',
            '''            <p className="mt-10 text-sm font-bold">Cover</p>
            <PlanFeatureNotice feature="customCover" label="Custom cover templates" />
            <div className="mt-5 grid grid-cols-2 gap-x-4 gap-y-8">
              {adminCoverTemplates.map((template) => (
                <button
                  key={template.id}
                  className={cn("relative text-center", customCoverAccess.locked && "cursor-not-allowed opacity-45")}
                  disabled={customCoverAccess.locked}
                  onClick={() => onChange({ cover: `custom:${template.id}`, customCoverTemplate: template } as Partial<typeof design>)}
                  type="button"
                >''',
            "custom covers disabled")
          text = replace_once(text,
            '''        {activePanel === "typography" && (
          <>''',
            '''        {activePanel === "typography" && (
          <PlanFeatureLock feature="advancedDesign" label="Advanced design">''',
            "typography lock open")
          text = replace_once(text,
            '''          </>
        )}

        {activePanel === "color" && (
          <>''',
            '''          </PlanFeatureLock>
        )}

        {activePanel === "color" && (
          <PlanFeatureLock feature="advancedDesign" label="Advanced design">''',
            "typography close color open")
          text = replace_once(text,
            '''          </>
        )}

        {activePanel === "grid" && (
          <>''',
            '''          </PlanFeatureLock>
        )}

        {activePanel === "grid" && (
          <PlanFeatureLock feature="layouts" label="Layouts">''',
            "color close grid open")
          text = replace_once(text,
            '''          </>
        )}
    </div>''',
            '''          </PlanFeatureLock>
        )}
    </div>''',
            "grid close")

          # Download PIN and limit controls remain visible but disabled.
          text = replace_once(text,
            "}) {\n  return (\n    <div className=\"max-w-[620px]\">",
            "}) {\n  const pinAccess = usePlanFeatureAccess(\"pinSet\");\n  const limitAccess = usePlanFeatureAccess(\"downloadLimit\");\n  return (\n    <div className=\"max-w-[620px]\">",
            "download feature access")
          text = replace_once(text,
            '''        {[
          ["Video Download", "videoDownload", "Allow videos to be downloaded for offline viewing."],
          ["Download PIN", "downloadPin", "If enabled, all collections created from this preset will have a download PIN set automatically."],
        ].map(([label, key, text]) => (
          <Field key={key}>''',
            '''        <PlanFeatureNotice feature="pinSet" label="Download PIN" />
        {[
          ["Video Download", "videoDownload", "Allow videos to be downloaded for offline viewing."],
          ["Download PIN", "downloadPin", "If enabled, all collections created from this preset will have a download PIN set automatically."],
        ].map(([label, key, text]) => (
          <Field key={key} className={cn(key === "downloadPin" && pinAccess.locked && "pointer-events-none opacity-45")}>''',
            "pin notice and field")
          text = replace_once(text,
            '''               <Switch
                 checked={download[key as "videoDownload" | "downloadPin"]}
                 onCheckedChange={(value) =>''',
            '''               <Switch
                 disabled={key === "downloadPin" && pinAccess.locked}
                 checked={download[key as "videoDownload" | "downloadPin"]}
                 onCheckedChange={(value) =>''',
            "pin switch disabled")
          text = replace_once(text,
            '''        <div>
          <p className="mb-8 text-[11px] font-bold uppercase tracking-widest text-[#777]">''',
            '''        <div>
          <PlanFeatureNotice feature="downloadLimit" label="Download limits" />
          <p className="mb-8 text-[11px] font-bold uppercase tracking-widest text-[#777]">''',
            "download limit notice")
          text = replace_once(text,
            '''              <Field key={key}>
                <FieldLabel className="font-bold">{label}</FieldLabel>''',
            '''              <Field key={key} className={cn(limitAccess.locked && "pointer-events-none opacity-45")}>
                <FieldLabel className="font-bold">{label}</FieldLabel>''',
            "download limit fields")
          text = replace_once(text,
            '''                  <Switch
                    checked={download[key as "restrictDownloads" | "limitDownloads"]}''',
            '''                  <Switch
                    disabled={limitAccess.locked}
                    checked={download[key as "restrictDownloads" | "limitDownloads"]}''',
            "download limit switch")

          # Store preset is visible but disabled without Store feature.
          text = replace_once(text,
            '''  return (
    <div className="max-w-[560px]">
      <h2 className="text-2xl font-medium">Store</h2>''',
            '''  return (
    <PlanFeatureLock feature="store" label="Store">
    <div className="max-w-[560px]">
      <h2 className="text-2xl font-medium">Store</h2>''',
            "store preset lock open")
          text = replace_once(text,
            '''    </div>
  );
}

function PresetPager''',
            '''    </div>
    </PlanFeatureLock>
  );
}

function PresetPager''',
            "store preset lock close")

          # Free plan activation and button wording.
          text = replace_once(text,
            '  const [error, setError] = useState("");\n',
            '  const [error, setError] = useState("");\n  const [notice, setNotice] = useState("");\n',
            "storage plan notice")
          text = replace_once(text,
            '''        const result = await checkoutPlan(planId);
        if (result.checkoutUrl) window.location.href = result.checkoutUrl;
        else setError("Stripe checkout URL missing");''',
            '''        const result = await checkoutPlan(planId);
        if (result.checkoutUrl) window.location.href = result.checkoutUrl;
        else if (result.activated) {
          setData(await getBillingOverview());
          setNotice("Free plan activated successfully");
        } else setError("Plan activation failed");''',
            "free plan frontend activation")
          text = replace_once(text,
            '''      {error && <p className="mt-5 border-l-2 border-red-500 pl-3 text-sm font-semibold text-red-600">{error}</p>}''',
            '''      {error && <p className="mt-5 border-l-2 border-red-500 pl-3 text-sm font-semibold text-red-600">{error}</p>}
      {notice && <p className="mt-5 border-l-2 border-[#22bda7] pl-3 text-sm font-semibold text-[#008f80]">{notice}</p>}''',
            "free plan notice render")
          text = replace_once(text,
            '''              disabled={pending || !Number(plan.priceMonthly ?? 0)}
              onClick={() => buyPlan(plan._id)}
            >
              {pending ? <Loader2 className="size-4 animate-spin" /> : "Buy Plan"}''',
            '''              disabled={pending}
              onClick={() => buyPlan(plan._id)}
            >
              {pending ? <Loader2 className="size-4 animate-spin" /> : Number(plan.priceMonthly ?? 0) <= 0 ? "Activate Free" : "Buy Plan"}''',
            "free plan button")
          write(path, text)

          # PlanFeatureLock bypass support used for ordinary client pages.
          path = "frontend/components/dashboard/plan-feature-lock.tsx"
          text = read(path)
          text = text.replace("  className,\n}: {", "  className,\n  bypass = false,\n}: {")
          text = text.replace("  className?: string;\n}) {", "  className?: string;\n  bypass?: boolean;\n}) {")
          text = text.replace("  const { locked, capabilities } = usePlanFeatureAccess(feature);", "  const access = usePlanFeatureAccess(feature);\n  const locked = !bypass && access.locked;\n  const capabilities = access.capabilities;")
          write(path, text)

          # Remove temporary workflow before the grouped product commit.
          Path(".github/workflows/finalize-admin-plan-features.yml").unlink()
          PY

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Build backend
        working-directory: backend
        run: npm ci && npm run build

      - name: Build frontend
        working-directory: frontend
        env:
          BASE_URL: http://127.0.0.1:4000
          NEXT_PUBLIC_BASE_URL: http://127.0.0.1:4000
        run: npm ci && npm run build

      - name: Commit grouped implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: add admin template management and plan feature access"
          git push origin HEAD:development
